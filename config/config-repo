spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: 
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9001/eureka/
server:
  port: 10000
