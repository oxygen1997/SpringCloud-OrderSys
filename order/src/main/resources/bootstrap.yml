spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG #组件config的服务名字
      profile: dev
      label: master
      uri: http://localhost:9999





